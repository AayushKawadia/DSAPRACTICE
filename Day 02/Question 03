#Hollow Square pattern

#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter the size of the square (n): ";
    cin >> n;

    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            // Print '*' for the border, otherwise print a space
            if(i == 0 || i == n - 1 || j == 0 || j == n - 1) {
                cout << "*";
            }
            else {
                cout << " ";
            }
        }
        cout << endl; // Move to the next line after each row
    }

    return 0;
}

Output:
For n = 5, the output will be:
*****
*   *
*   *
*   *
*****
